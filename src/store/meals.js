import meals from './meals.json'

// ------------------------------------
// Reducer
// ------------------------------------
const initialState = meals.meals

export default function mealReducer (state = initialState, action) {
  return state
}
